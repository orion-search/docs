import useBaseUrl from '@docusaurus/useBaseUrl';

Orion combines a semantic search model with a traditional keyword search engine. Users can query Orion with anything from a keyword or phrase (for example, _gene editing_) to an entire paragraph of text and Orion's search engine will find semantically similar academic publications.

<img alt="Orion  search engine" src={useBaseUrl('img/search-engine-diagram.png')} />
